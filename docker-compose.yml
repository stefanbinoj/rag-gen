version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: question-generator-app
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
